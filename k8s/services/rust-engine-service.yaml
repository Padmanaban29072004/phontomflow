apiVersion: v1
kind: Service
metadata:
  name: rust-engine
  namespace: phantom-flow
  labels:
    app.kubernetes.io/name: phantom-flow
    app.kubernetes.io/component: rust-engine
spec:
  selector:
    app.kubernetes.io/component: rust-engine
  ports:
    - name: http
      port: 9090
      targetPort: http
      protocol: TCP
  type: ClusterIP
