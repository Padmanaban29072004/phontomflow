apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: phantom-flow
  labels:
    app.kubernetes.io/name: phantom-flow
    app.kubernetes.io/component: ml-service
spec:
  selector:
    app.kubernetes.io/component: ml-service
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  type: ClusterIP
